export default function menuPager() {  
  const container = document.querySelector(".container");

  container.textContent = "sorry i don't know any coffee names :S"

  const p = document.createElement("p")

  p.textContent
}
